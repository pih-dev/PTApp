# SpotSet v2.35.2 — the prone stability ball stays under the client

**Date:** 2026-08-23 · Figures session (CCHealth/Fable). The v2.35.1 split-grip ball rule fired on
any grip gap >120, which caught the two prone stability-ball movements (Prone Y Raise, Prone
Scapular Retraction — lateral-raise archetype, symmetric level arms) and moved the ball the client
lies ON into a raised hand. Found by the `mobile-ux-reviewer` pass over the release, not by a user.
The one-hand anchor now additionally requires the grips to differ in height (>60 canon units);
level-and-wide keeps the centre. The prone pair's near-head centre is imperfect but pre-existing —
queued for a judging round, with the rollout fault ball's 20-unit right-edge overflow and Offset
Barbell Press spinning as a symmetric bar (contradicts its asymmetric-load teaching point).
